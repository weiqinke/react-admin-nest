import React from "react";
import IconFont from "../IconFont";

const CreateIcon = ({ icon = "icon-shouye" }) => {
  return <IconFont type={icon} className="nexticon" />;
};

export default CreateIcon;
